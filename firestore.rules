rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Angkringan POS security rules: allow read/write for operational POS sync
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
